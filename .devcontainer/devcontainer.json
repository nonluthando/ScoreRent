{
  "name": "ScoreRent (FastAPI + Postgres)",
  "dockerComposeFile": "docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/workspaces/scorerent",
  "forwardPorts": [4000, 5432],
  "postCreateCommand": "pip install -r requirements.txt"
}
